class UserEntity {
  final String? name, email, password, phone, country, id, image, location, bio;
  const UserEntity({
    this.id,
    this.name,
    this.email,
    this.password,
    this.phone,
    this.bio,
    this.country,
    this.image,
    this.location,
  });
}
